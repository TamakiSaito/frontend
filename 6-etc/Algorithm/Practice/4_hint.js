// 問題1: 配列の最大値を見つける
// 式：配列の最大値は、Math.max(...arr) またはループを使って求められます。
// Math.max(...arr); // スプレッド構文で配列内の最大値を取得

// 問題2: 文字列の逆順
// 式：文字列を逆順にするには、split、reverse、join の組み合わせを使います。
// str.split('').reverse().join(''); // 文字列を配列にし、逆順にしてから再び文字列に

// 問題3: 配列の平均
// 式：平均は、合計を配列の長さで割ることで求められます。
// arr.reduce((sum, num) => sum + num, 0) / arr.length; // 合計 / 要素数

// 問題4: 文字列の母音数をカウントする
// 式：母音に該当するかを判定しながらカウントを進めます。
// str.split('').filter(char => 'aeiouAEIOU'.includes(char)).length; // 母音のみを数える

// 問題5: 指定した文字の出現回数をカウントする
// 式：filter メソッドで特定の文字をカウントします。
// str.split('').filter(char => char === targetChar).length; // 指定文字の数

// 問題6: 数字が回文かどうか判定する
// 式：数値を文字列に変換し、逆順にして比較します。
// String(num) === String(num).split('').reverse().join(''); // 元の文字列と逆順にしたものが一致するか

// 問題7: 偶数か奇数かを判定する
// 式：偶数は num % 2 === 0 で判定できます。
// num % 2 === 0; // true なら偶数、false なら奇数